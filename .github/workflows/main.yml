on:
  push:
    branches:
        - master



jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Before Printing folder contents'
        run: ls

      - uses: actions/checkout@v3  

      - name: 'Before Printing folder contents'
        run: ls

        
        

